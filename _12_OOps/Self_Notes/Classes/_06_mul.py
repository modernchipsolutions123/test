class mul:
    #1.__init __ method is used to initialize the variables
    def __init__(self, a, b,c):
        self.a = a
        self.b = b   #FIELDS
        self.c = c

    def display(self): #2.methods3ion of two numbers....")
        # print(self.a*self.b)
        return self.a * self.b

s = mul(10, 20)
print(s.display())
